@model IEnumerable<Sauraav_POE_CLDV.Models.carBodyTypeModel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.carBodyTypeDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.carBodyTypeDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.carBodyTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.carBodyTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.carBodyTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
